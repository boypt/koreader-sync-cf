{
  "name": "koreader-sync-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "deploy": "npx wrangler publish",
    "build": "echo 'no build step required'"
  },
  "engines": {
    "node": ">=16"
  }
  ,
  "devDependencies": {
    "wrangler": "^3.5.0"
  }
}
